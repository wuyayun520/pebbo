
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let noti_originalPath:String = "getDefoundation import"
fileprivate let controllerFinishName:String = "Iexclude"

/*: "getFirebaseID" :*/
fileprivate let constCurfewStatus:String = "progress window formatgetF"
fileprivate let kDocumentId:String = "seIDplatform show white register ting"

/*: "getAreaISO" :*/
fileprivate let main_deviceKey:[Character] = ["g","e","t","A","r"]
fileprivate let managerReactionVersion:String = "eaISOzone original"

/*: "getProxyStatus" :*/
fileprivate let viewMicStatus:[Character] = ["g","e","t","P","r","o","x","y","S","t","a"]
fileprivate let routerLaterActionPath:[Character] = ["t","u","s"]

/*: "getMicStatus" :*/
fileprivate let helperRemoteScreenStatus:[Character] = ["g","e","t","M","i","c","S","t","a","t","u","s"]

/*: "getPhotoStatus" :*/
fileprivate let show_mainTime:String = "getPindicator country grant scene"
fileprivate let engineLogState:String = "Statusthe stop value"

/*: "getCameraStatus" :*/
fileprivate let sessionOrganizeKey:String = "scene sound take mask textgetC"
fileprivate let configDomainValue:String = "handtus"

/*: "reportAdjust" :*/
fileprivate let sessionWealthyPersonID:String = "commandepo"

/*: "requestLocalPush" :*/
fileprivate let routerTransportResult:[Character] = ["r","e","q","u","e","s","t","L","o","c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let modelCameraString:[Character] = ["g","e","t","L","a","n","g"]
fileprivate let factoryBackDate:String = "system main interval remoteCode"

/*: "getTimeZone" :*/
fileprivate let dataTrademarkUrl:String = "latert"
fileprivate let filterNeverString:String = "Timesecret time permission var"

/*: "getInstalledApps" :*/
fileprivate let modelUtilisationPath:String = "getInswhite confirm bridge block"
fileprivate let const_reError:String = "talltext"
fileprivate let kExcludeDict:String = "dAppsreturn region"

/*: "getSystemUUID" :*/
fileprivate let user_modeValue:String = "foundatione"
fileprivate let notiTelecommunicationData:[Character] = ["t","S","y","s","t","e","m","U","U","I","D"]

/*: "getCountryCode" :*/
fileprivate let userPleaseError:String = "and capture net startgetCou"
fileprivate let const_sessionSecret:[Character] = ["n","t","r","y","C","o","d","e"]

/*: "inAppRating" :*/
fileprivate let const_presentId:String = "inAppRand trust"
fileprivate let loggerObserverState:String = "native"

/*: "apPay" :*/
fileprivate let routerHappeningMessage:[Character] = ["a","p","P","a","y"]

/*: "subscribe" :*/
fileprivate let controllerDecisionStatus:String = "superson"
fileprivate let sessionServerTime:String = "criprocess"

/*: "openSystemBrowser" :*/
fileprivate let viewCurrentTitle:String = "openSystorigin rich center panel number"
fileprivate let show_schemeURL:[Character] = ["e","m","B","r","o","w","s","e","r"]

/*: "closeWebview" :*/
fileprivate let kitNetworkMsg:String = "CLOS"
fileprivate let userEndModeName:[Character] = ["w"]

/*: "openNewWebview" :*/
fileprivate let configCountIndexContactUrl:[Character] = ["o","p","e","n","N","e","w","W"]
fileprivate let userIndicatorToResult:String = "EBVIEW"

/*: "reloadWebview" :*/
fileprivate let viewTransactionManagerData:[Character] = ["r","e","l","o","a","d","W","e"]
fileprivate let remoteEveryData:[Character] = ["b","v","i","e","w"]

/*: "typeName" :*/
fileprivate let loggerObserverStr:[UInt8] = [0x28,0x25,0x2c,0x39,0x12,0x3d,0x31,0x39]

/*: "deviceID" :*/
fileprivate let main_withoutCount:[UInt8] = [0x5c,0x5d,0x4e,0x51,0x5b,0x5d,0x71,0x7c]

private func canPoint(enter num: UInt8) -> UInt8 {
    return num ^ 56
}

/*: "fireBaseID" :*/
fileprivate let sessionResponseToken:[UInt8] = [0x18,0x17,0xc,0x1b,0x3c,0x1f,0xd,0x1b,0x37,0x3a]

private func maxCapture(label num: UInt8) -> UInt8 {
    return num ^ 126
}

/*: "areaISO" :*/
fileprivate let userArrayRevenueFormat:[UInt8] = [0xe3,0xf0,0xe7,0xe3,0xcb,0xd1,0xcd]

private func grossNo(document num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "isProxy" :*/
fileprivate let dataPrivacyUrl:[UInt8] = [0x79,0x78,0x6f,0x72,0x50,0x73,0x69]

/*: "langCode" :*/
fileprivate let k_statusTime:[UInt8] = [0x65,0x64,0x6f,0x43,0x67,0x6e,0x61,0x6c]

/*: "timeZone" :*/
fileprivate let parserPolicyClipValue:[UInt8] = [0x5f,0x42,0x46,0x4e,0x71,0x44,0x45,0x4e]

private func fatalWill(multi num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "installedApps" :*/
fileprivate let main_arrowMessage:[UInt8] = [0x3e,0x39,0x24,0x23,0x36,0x3b,0x3b,0x32,0x33,0x16,0x27,0x27,0x24]

private func mediaSum(command num: UInt8) -> UInt8 {
    return num ^ 87
}

/*: "systemUUID" :*/
fileprivate let appPostRatingID:[UInt8] = [0x44,0x49,0x55,0x55,0x6d,0x65,0x74,0x73,0x79,0x73]

/*: "countryCode" :*/
fileprivate let engineFootingStatus:[UInt8] = [0x42,0x4e,0x54,0x4f,0x55,0x53,0x58,0x62,0x4e,0x45,0x44]

private func blockUser(campaign num: UInt8) -> UInt8 {
    return num ^ 33
}

/*: "status" :*/
fileprivate let constContentDict:[UInt8] = [0x57,0x58,0x45,0x58,0x59,0x57]

fileprivate func documentDevice(reaction num: UInt8) -> UInt8 {
    let value = Int(num) + 28
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isAuth" :*/
fileprivate let const_facilityMessage:[UInt8] = [0x43,0x59,0x6b,0x5f,0x5e,0x42]

/*: "isFirst" :*/
fileprivate let configReplaceAddCountMessage:[UInt8] = [0xae,0xb8,0x8b,0xae,0xb7,0xb8,0xb9]

fileprivate func publicTransportInventory(error num: UInt8) -> UInt8 {
    let value = Int(num) + 187
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let userPostTitle:[Character] = ["_","_","L","o","c","a","l","P","u","s","h"]

/*: "identifier" :*/
fileprivate let data_titleResult:[UInt8] = [0x8d,0x80,0x81,0x8a,0x90,0x8d,0x82,0x8d,0x81,0x96]

private func handLab(fatal num: UInt8) -> UInt8 {
    return num ^ 228
}

/*: "HTTPProxy" :*/
fileprivate let loggerDataModeSecret:String = "HTTcountry"
fileprivate let sessionScreenName:String = "rocore"

/*: "HTTPSProxy" :*/
fileprivate let main_upTime:[Character] = ["H"]
fileprivate let user_rootUrl:[Character] = ["T","T","P","S","P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let controllerTransactionState:[Character] = ["S","O","C","K","S","P","r","o"]
fileprivate let show_decisionGlobalString:String = "origin"

/*: "__SCOPED__" :*/
fileprivate let showEventId:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let routerHeadKey:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let uSGovernmentName:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let cacheWindowFrameReactionError:String = "objectsec"

/*: "ppp" :*/
fileprivate let viewLogAcrossData:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let helperUnfinishedStatus:String = "command response region end requireRetry"
fileprivate let constPrevalenceData:String = "permission base usr or "
fileprivate let configGenerateRawSecret:[Character] = [" "]

/*: "Feedback" :*/
fileprivate let data_fullError:[Character] = ["F","e","e","d","b","a","c","k"]

/*: " to contact us" :*/
fileprivate let dataPhoneURL:[Character] = [" ","t","o"," ","c","o","n","t","a","c","t"," ","u"]
fileprivate let showCurrencyDate:[Character] = ["s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let routerTimeStr = (String(noti_originalPath.prefix(5)) + "vice" + controllerFinishName.replacingOccurrences(of: "exclude", with: "D")) // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let managerFurnishShowSecret = (String(constCurfewStatus.suffix(4)) + "ireba" + String(kDocumentId.prefix(4))) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let viewOldExcludeError = (String(main_deviceKey) + String(managerReactionVersion.prefix(5))) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let noti_photoToken = (String(viewMicStatus) + String(routerLaterActionPath)) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let loggerLocalSecret = (String(helperRemoteScreenStatus)) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let controllerShowMessage = (String(show_mainTime.prefix(4)) + "hoto" + String(engineLogState.prefix(6))) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let cacheScheduleNumberList = (String(sessionOrganizeKey.suffix(4)) + "ameraS" + configDomainValue.replacingOccurrences(of: "hand", with: "ta")) // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let helperMacId = (sessionWealthyPersonID.replacingOccurrences(of: "command", with: "r") + "rtAdjust") // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let pointBodyTitle = (String(routerTransportResult)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let userLongshotKey = (String(modelCameraString) + String(factoryBackDate.suffix(4))) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let show_keyMessage = (dataTrademarkUrl.replacingOccurrences(of: "later", with: "ge") + String(filterNeverString.prefix(4)) + "Zone") // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let networkReplaceVersion = (String(modelUtilisationPath.prefix(6)) + const_reError.replacingOccurrences(of: "text", with: "e") + String(kExcludeDict.prefix(5))) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let userInstallState = (user_modeValue.replacingOccurrences(of: "foundation", with: "g") + String(notiTelecommunicationData)) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let engineSessionPath = (String(userPleaseError.suffix(6)) + String(const_sessionSecret)) // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let constAreaSecret = (String(const_presentId.prefix(6)) + "atin" + loggerObserverState.replacingOccurrences(of: "native", with: "g")) // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let main_maxVersion = (String(routerHappeningMessage)) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let viewMakeData = (controllerDecisionStatus.replacingOccurrences(of: "person", with: "bs") + sessionServerTime.replacingOccurrences(of: "process", with: "be")) // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let userMacID = (String(viewCurrentTitle.prefix(8)) + String(show_schemeURL)) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let main_versionMessage = (kitNetworkMsg.lowercased() + "eWebvie" + String(userEndModeName)) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let main_zoneId = (String(configCountIndexContactUrl) + userIndicatorToResult.lowercased()) // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let k_pointValue = (String(viewTransactionManagerData) + String(remoteEveryData)) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct ActiveFollow: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: MethodLost?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct MethodLost: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension AttenderCombine {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func requestRating(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = ActiveFollow.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case routerTimeStr:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = EdgeQuantityry.theZero()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: main_withoutCount.map{canPoint(enter: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case managerFurnishShowSecret:
                //: AppWebViewController.getFireBaseToken { str in
                AttenderCombine.hand { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: sessionResponseToken.map{maxCapture(label: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case viewOldExcludeError:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = AttenderCombine.empty()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: userArrayRevenueFormat.map{grossNo(document: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case noti_photoToken:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = AttenderCombine.error()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: dataPrivacyUrl.reversed(), encoding: .utf8)!: status])

            //: case getLangCode:
            case userLongshotKey:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: k_statusTime.reversed(), encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case show_keyMessage:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: parserPolicyClipValue.map{fatalWill(multi: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case networkReplaceVersion:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: main_arrowMessage.map{mediaSum(command: $0)}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case userInstallState:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: appPostRatingID.reversed(), encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case engineSessionPath:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: engineFootingStatus.map{blockUser(campaign: $0)}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case constAreaSecret:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                AttenderCombine.petition()

            //: case apPay:
            case main_maxVersion:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.head(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: constContentDict.map{documentDevice(reaction: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case viewMakeData:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.head(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: constContentDict.map{documentDevice(reaction: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case userMacID:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case main_versionMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.atHandWill()

            //: case openNewWebview:
            case main_zoneId:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    AttenderCombine.processIn(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case k_pointValue:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.interval()

            //: case getMicStatus:
            case loggerLocalSecret:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                BoundaryMessage.shared.usage { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: const_facilityMessage.map{$0^42}, encoding: .utf8)!: auth, String(bytes: configReplaceAddCountMessage.map{publicTransportInventory(error: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case controllerShowMessage:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                BoundaryMessage.shared.afterLaissezPasserWill { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: const_facilityMessage.map{$0^42}, encoding: .utf8)!: auth, String(bytes: configReplaceAddCountMessage.map{publicTransportInventory(error: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case cacheScheduleNumberList:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                BoundaryMessage.shared.contentBy { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName, String(bytes: const_facilityMessage.map{$0^42}, encoding: .utf8)!: auth, String(bytes: configReplaceAddCountMessage.map{publicTransportInventory(error: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case helperMacId:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        EdgeQuantityry.compound(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        EdgeQuantityry.track(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case pointBodyTitle:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: loggerObserverStr.map{$0^92}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                AttenderCombine.exceptEnableBridge(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension AttenderCombine {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func processIn(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = AttenderCombine()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        RingDisk.getWeaving()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func exceptEnableBridge(_ model: ActiveFollow) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(sessionOfferMode)" + (String(userPostTitle))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: data_titleResult.map{handLab(fatal: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func hand(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func empty() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func error() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if AttenderCombine.trackInstance() || AttenderCombine.publicTransport() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func trackInstance() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(loggerDataModeSecret.replacingOccurrences(of: "country", with: "PP") + sessionScreenName.replacingOccurrences(of: "core", with: "xy"))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(main_upTime) + String(user_rootUrl))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(controllerTransactionState) + show_decisionGlobalString.replacingOccurrences(of: "origin", with: "xy"))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func publicTransport() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(showEventId))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(routerHeadKey))) || keyStr.contains((String(uSGovernmentName))) || keyStr.contains((cacheWindowFrameReactionError.replacingOccurrences(of: "object", with: "ip"))) || keyStr.contains((String(viewLogAcrossData))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func petition() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func head(productId: String, source: Int = -1, payType: FirstPartner, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        ProdView.sharedAcross()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        ManagingDirectorDomain.shared.windowBuild(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            ProdView.color()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    ProdView.blockInstance((String(helperUnfinishedStatus.suffix(5)) + " Afte" + String(constPrevalenceData.suffix(5)) + "Go to" + String(configGenerateRawSecret)) + "\"" + (String(data_fullError)) + "\"" + (String(dataPhoneURL) + String(showCurrencyDate)))

                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.decision()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
